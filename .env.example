# ============================================================
# CONFIGURAÇÃO DO AGENTE IA LOCAL
# ============================================================
# Copie este arquivo para .env e ajuste conforme necessário
# cp .env.example .env

# ============================================================
# OLLAMA (Servidor Local de LLM)
# ============================================================
# URL do servidor Ollama rodando localmente
# Padrão: http://localhost:11434
OLLAMA_HOST=http://localhost:11434

# Modelo LLM principal para planejamento e execução
# Modelos recomendados: qwen:14b, qwen-coder-2.5, mistral, neural-chat
# Use qwen-coder-2.5 para tarefas de código
OLLAMA_MODEL=qwen-coder-2-5 #qwen:14b

# Modelo para embeddings (RAG/Memory)
# Modelos compatíveis: nomic-embed-text (4GB), all-minilm (22MB)
# nomic-embed-text é de melhor qualidade, all-minil'm é mais rápido
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# ============================================================
# COMPORTAMENTO DOS AGENTES
# ============================================================
# Temperatura (0.0 = determinístico, 1.0 = criativo)
# Use 0.2 para tarefas técnicas, 0.7 para criativas
LLM_TEMPERATURE=0.2

# Número máximo de tentativas de retry em caso de erro
MAX_RETRIES=2

# Timeout para chamadas ao Ollama (segundos)
LLM_TIMEOUT=60

# ============================================================
# ARMAZENAMENTO DE MEMÓRIA (RAG)
# ============================================================
# Diretório para banco de dados ChromaDB
# Armazena embeddings de contexto passado
MEMORY_DB_PATH=./vector_store/chroma_db

# Número máximo de resultados para trazer na busca de similaridade
MEMORY_TOP_K=5

# ============================================================
# SISTEMA DE ARQUIVOS
# ============================================================
# Caminho raiz do projeto (não altere se estiver rodando do root)
PROJECT_ROOT=.

# Arquivos/pastas excludidas de operações
# Separar por vírgula
EXCLUDED_PATHS=.git,.env,.venv,__pycache__,node_modules

# ============================================================
# LOGGING
# ============================================================
# Nível de log: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Salvar logs em arquivo? (yes/no)
LOG_TO_FILE=yes

# ============================================================
# GIT
# ============================================================
# Cometer automaticamente mudanças após executor?
AUTO_COMMIT=yes

# Nome do autor para commits
GIT_AUTHOR_NAME=AI Agent

# Email do autor para commits
GIT_AUTHOR_EMAIL=agent@localhost

# ============================================================
# LIMITES DE SEGURANÇA
# ============================================================
# Comandos terminais proibidos (separado por vírgula)
FORBIDDEN_COMMANDS=rm -rf,sudo,su,format,diskpart

# Máximo de linhas que pode ler por arquivo
MAX_FILE_READ_LINES=10000

# Máximo de arquivos que pode listar por diretório
MAX_DIR_LIST_FILES=1000
